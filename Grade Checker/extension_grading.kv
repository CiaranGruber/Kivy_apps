BoxLayout:
    #orientation: 'vertical'
    orientation: 'horizontal'
    BoxLayout:
        orientation: 'vertical'
        size_hint_x: 0.3
        Button:
            text: 'Clear'
            on_press: app.clear_all()
        Button:
            text: 'Calculate Grade'
            on_press: app.calculate_grade()
    BoxLayout:
        orientation: 'vertical'
        TextInput:
            id: input_grade
            text: ''
        Label:
            id: output_label
            text: 'Enter your grade'
            color: (1, 1, 0, 1)